export const metadata = {
  title: "Rama Empresarial",
};

export default function RootLayout({ children }) {
  return (
    <div>
        {children}
        <link rel="canonical" href="https://www.principiosyvaloressantafe.com.ar/arbol/rama-empresarial"></link>

    </div>
  );
}
